# PHI runtime and test-fixture dependencies
faker>=25.0.0

# Document readers and fixture-authoring file formats (stress/gateway fixtures)
openpyxl>=3.1.5
python-docx>=1.1.2
pypdf>=5.0.1
reportlab>=4.2.5
pydicom>=3.0.1
Pillow>=11.0.0
piexif>=1.1.3

# LLM provider SDKs (install the one(s) you use)
httpx>=0.27.0                  # required for Ollama local API
openai>=1.50.0                 # required for openai and openai-oauth providers
# anthropic>=0.40.0            # uncomment for Claude (Anthropic API)
# google-generativeai>=0.8.0   # uncomment for Gemini

# PHI detection (Presidio residual-control gate; runtime dependency)
presidio-analyzer>=2.2.355
spacy>=3.8.0,<3.9.0
# boto3>=1.35.0  # uncomment for AWS Comprehend Medical
# azure-identity>=1.19.0  # uncomment for Azure Health

# Data handling
pandas>=2.2.3
numpy>=2.1.3

# Testing
pytest>=8.3.3
pytest-cov>=6.0.0
pytest-timeout>=2.3.1
hypothesis>=6.115.5
PyYAML>=6.0.0  # tests/test_xls_dependency_contract.py parses .github/workflows/ci.yml

# Security
cryptography>=43.0.3

# phi_engine additional dependencies
pycanon>=1.0.1
click>=8.0.0

# Standalone organizer -- messy-input ingestion
pdfplumber>=0.11
xlrd==2.0.2
# xlwt is BIFF-only (legacy .xls writer); used solely by the stress-fixture
# builder (harness/make_stress_fixtures.py) to author a genuine .xls sample.
xlwt>=1.3.0
defusedxml>=0.7.1  # xlsx header extraction -- guards zipfile/XML parsing
