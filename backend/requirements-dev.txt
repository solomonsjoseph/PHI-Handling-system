# Dev/test-only tooling for the PHI Console backend: linters, formatter,
# type checker, and parallel pytest runner. reportlab moved to
# requirements.txt: it is a production dependency now that it renders
# the release-required PDF reports (docs #58), not just the manual
# tests/live/intake_audit.py script.
# Not installed in the production image.
-r requirements.txt
black==26.5.1
flake8==7.3.0
isort==8.0.1
mypy==2.3.0
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-xdist==3.8.0
ruff==0.16.0

# Root phi_engine/tests suite deps (Step 0): faker generates synthetic
# adversarial corpus data, xlwt writes legacy .xls fixtures the root
# suite exercises against phi_engine's XLS-rejection path. Not imported
# by backend/ itself.
faker==40.37.0
xlwt==1.3.0
